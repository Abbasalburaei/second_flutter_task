import 'package:flutter/material.dart';

class NoteModel {
  final String title;
  final String subTitle;
  final Color? backgroundColor;
  NoteModel(this.title, this.subTitle, this.backgroundColor);
}
